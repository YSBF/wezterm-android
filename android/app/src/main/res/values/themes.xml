<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- GameActivity derives from AppCompatActivity, so the theme has to be an
         AppCompat one. NoActionBar because the terminal draws its own tab bar
         and wants the whole surface. -->
    <style name="Theme.WezTerm" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
